# Core Modules

OS core settings.

## Modules

- **`i18n.nix`**: Locale settings behind `my.core.i18n.enable` (default on) — `ja_JP.UTF-8` / `en_US.UTF-8`, console keymap (`us`).
- **`default.nix`**: Imports the core modules.
